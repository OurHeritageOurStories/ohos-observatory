import { Ref } from "vue";
import { Position } from '../common/types';
import { SimpleLayout } from "./simple";
export declare type GridLayoutParameters = {
    grid?: number;
};
export declare class GridLayout extends SimpleLayout {
    private options;
    constructor(options?: GridLayoutParameters);
    protected setNodePosition(nodeLayout: Ref<Position>, pos: Position): void;
}
