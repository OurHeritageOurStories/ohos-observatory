import type { ComputedRef, Ref } from 'vue';
import { ViewConfig } from '../common/configs';
interface ZoomProvides {
    zoomLevel: Ref<number>;
    scale: Ref<number>;
}
export declare function provideZoomLevel(zoomLevel: Ref<number>, viewStyle: ViewConfig): {
    scale: ComputedRef<number>;
};
export declare function useZoomLevel(): ZoomProvides;
export {};
