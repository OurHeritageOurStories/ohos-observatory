import { MarkerStyle } from '../common/configs';
export declare type MarkerBuilder = (marker: MarkerStyle | null, isSource?: boolean) => string;
export interface HeadMarker extends MarkerStyle {
    color: string;
    isSource: boolean;
}
export interface MarkerState {
    markers: Record<string, HeadMarker>;
    referenceCount: Record<string, number>;
}
export declare function makeMarkerState(): MarkerState;
export declare function useMarker(markerState: MarkerState): {
    makeMarker: (marker: MarkerStyle, isSource: boolean, previousId: string | undefined, strokeColor: string, instanceId: number) => string | undefined;
    clearMarker: (id: string | undefined) => void;
};
